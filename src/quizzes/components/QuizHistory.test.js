// Given a quiz have historical answers, when the components is mounted, then we show the history
// Given the score of each test taken, when the component is mounted, then we see a light stop convention coloring
// Given we have no quizzes taken, when the component is mounted, then we dont see the history
it("Avoid test fails for lack of tests", () => {});
